/*$(function () {
    $(window).on('scroll', function () {
        if ($(window).scrollTop() > 10) {
            $('.topnav').addClass('active');
        } else {
            $('.topnav').removeClass('active');
        }
    });
});
*/